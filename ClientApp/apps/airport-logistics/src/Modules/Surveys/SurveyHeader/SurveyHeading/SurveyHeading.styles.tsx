import { makeStyles } from '@material-ui/core';

export const styles = makeStyles(({ palette }) => ({
  heading: {
    fontSize: '22px',
  },
  description: {
    fontSize: '14px',
    marginLeft: '10px',
  },
}));