import { makeStyles } from '@material-ui/core';

export const styles = makeStyles(({ palette }) => ({
  container: {
    width: '500px',
  },
  row: {
    display: 'flex',
    alignItems: 'center',
    marginBottom: '5px',
  },
  icon: {
    marginRight: '10px',
  },
}));
