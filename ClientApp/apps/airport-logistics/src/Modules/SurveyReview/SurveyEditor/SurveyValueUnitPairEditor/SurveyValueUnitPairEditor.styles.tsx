import { makeStyles } from '@material-ui/core';

export const styles = makeStyles(() => ({
  container: {
    display: 'flex',
  },
  textfield: {
    minWidth: 230,
    marginRight: 20,
  },
  select: {
    minWidth: 150,
  },
}));
