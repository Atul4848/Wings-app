import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles({
  boldSubtitle: {
    fontWeight: 600,
  },
  title:{
    whiteSpace: 'nowrap',
    textOverflow: 'ellipsis',
    overflow: 'hidden',
  }
});
