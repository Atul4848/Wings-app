import React, { FC, Fragment } from 'react';
import { withStyles } from '@material-ui/core';

import { globalStyles } from './GlobalStyles.styles';

const GlobalStyles: FC = () => <Fragment />;

export default withStyles(globalStyles)(GlobalStyles);
