interface StateColors {
  default: string;
  hovered: string;
  checked: string;
  disabled: string;
};

export interface RadioPaletteOptions {
  border: StateColors;
  background: StateColors;
  icon: StateColors;
}
